file(GLOB_RECURSE SOURCE_FILES CONFIGURE_DEPENDS *.c)
file(GLOB_RECURSE HEADER_FILES CONFIGURE_DEPENDS *.h)

set(SRC_SOURCE_FILES ${SOURCE_FILES} PARENT_SCOPE)
set(SRC_HEADER_FILES ${HEADER_FILES} PARENT_SCOPE)